# Persistência: salva snapshot se ao menos 1 chave mudou em 60 segundos
save 60 1

# Diretório de dados (mapeado para volume Docker)
dir /data

# Log level
loglevel notice

# Tamanho máximo de memória (ajustar conforme servidor)
maxmemory 256mb
maxmemory-policy allkeys-lru
